#include<stdio.h>
#include<stdlib.h>


struct binaryNode{
	int data;
	struct binaryNode *parent;
	struct binaryNode *left;
	struct binaryNode *right;
	};
	
	
void insertNode(struct binaryNode **roof_ref){
	struct binaryNode *new_node = 
				(struct binaryNode *)malloc(sizeof(struct binaryNode));
	if(binaryNode*)
	}


int main(){
	struct binaryNode *root = NULL;
	return 0;
	}
